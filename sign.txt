||
SQL COMMANDS:
select * from emp
select ename,comm , 12*sal+comm from emp WHERE  ename='KING';
select ename as NAME, sal*12 as "ANNUAL SALARY" from emp
select ename||' '||'is a '||' '||job as Employees from emp
select distinct deptno from emp
select ename, job, deptno from emp where job='CLERK'
select ename, job, deptno from emp where ename='JAMES'
select ename, job, deptno, comm, sal from emp where sal<=comm
select ename, job, deptno, sal from emp where sal BETWEEN 1000 AND 1500
select ename, job, deptno from emp where ename like 'S%'
select ename, job, deptno from emp where ename like '_A%'
select ename, job, deptno,mgr from emp where mgr is  null
select ename, job, deptno ,sal from emp where sal<=1000 and job='CLERK'
select ename, job, deptno ,sal from emp where sal>=1000 or job='CLERK'
select ename, job, deptno ,sal from emp where job not in ('CLERK','SALESMAN','MANAGER')
select ename, job, deptno ,sal from emp where (job='CLERK' or job='PRESIDENT' )AND sal<=1000
select ename,job,hiredate from emp order by hiredate
select ename,job,hiredate from emp order by hiredate desc
select ename,job,hiredate from emp order by hiredate asc
select empno,ename, sal*12 annsal from emp order by annsal
select ename, job, sal, deptno from emp order by deptno , sal desc
select ename,job,sal from emp where LOWER(ename)='blake'
select ename, concat (ename,job), length (ename),instr(ename,'A') from emp where substr(job,1,5)= 'SALES'
select ename,job, sal,comm, mod(sal,comm)  from emp where job='SALESMAN'
select ename,deptno,(sysdate-hiredate)/7 weeks from emp where deptno =10
select ename , to_char(hiredate ,'fm dd month yyyy')hiredate from emp
select ename , to_char(sal ,'$99,000')salary from emp where ename='SCOTT'
SELECT ename, sal, comm, (sal*12)+nvl(comm,0) from emp
select ename,length(ename),job, length(job), nullif(length(ename),length(job)) from emp
select ename,length(ename),job, length(job), nullif(ename,job) from emp
select job,sal, DECODE(job, 'ANALYST', sal*1.1,'CLERK',sal*1.15,'MANAGER',sal*1.20,sal) REVISED_SALARY  FROM   emp;
select ename,nvl(to_char(mgr),'NO MANAGER')from emp where mgr is null
select ename,mgr from emp where mgr is null
Select ename,job,sal,comm, nvl2(comm,comm+sal,sal) from emp



